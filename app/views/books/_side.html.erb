<h2>New Book</h2>
<%= form_with model:book, local:true do |f| %>
<div class="field">
  <label>Title</label>
  <%= f.text_field :title %>
</div>

<div class="field">
  <lavel>Opinion</lavel>
  <%= f.text_area :body %>
</div>

<div class="actions">
<%= f.submit "Creat Book" %>
</div>
<% end %>
